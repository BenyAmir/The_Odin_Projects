<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.user %> |  <%= new Intl.DateTimeFormat('en-US').format(item.added) %></title>
    <link rel="stylesheet" type="text/css" href="../css/item.css"/>
    <link rel="stylesheet" type="text/css" href="./css/main.css"/>
</head>
<body>
    <header>
        <button type="button" class="back-button"><a href="/">Back</a></button>
    </header>
    <main>
    <h1><%= item.user %></h1>
    <h3><%= item.text %></h3>
    <time datetime="<%= item.added.toISOString() %>"><%= new Intl.DateTimeFormat('en-US').format(item.added) %></time>
</main>
</body>
</html>