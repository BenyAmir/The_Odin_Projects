<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link type="text/css" rel="stylesheet" href="/global.css" />
  </head>
  <body>
    <h1>Login to read posts</h1>
<div class="form-container">
    <form action="/auth/login" method="post">
      <div class="input-wrapper"><label for="username">Username</label>
      <input type="text" name="username" id="username" />
      </div>

      <div class="input-wrapper"><label for="password">Password</label>
      <input type="text" name="password" id="password" />
      </div>

      <% if(locals.errors){ locals.errors.map(err=>{ %>   
      <span class="error"><%= err %></span>
      <% })} %>

      <div class="form-buttons"><button type="button" onclick="window.location.href='/'">Back</button>
      <button type="submit">Submit</button>
    </form>
</div>
  </body>
</html>
